@extends('layouts.app')

@section('content')
    {{-- <div class="container">
        <div class="row justify-content-center">
            <div class="col-4">
                <div class="card">
                    <div class="card-img">
                        <img class="img-fluid" src="{{$user->image}}" alt="{{$user->name}}">
                    </div>
                    <div class="card-body">
                        <h3>{{$user->name}}</h3>
                        <p>{{$user->email}}</p>
                        <p>{{$user->created_at}}</p>
                    </div>
                </div>
            </div>
        </div>
    </div> --}}
@endsection
